# 留空 = 與目前頁面同源（經 Nginx 時不必設；例如 https://localhost:443 或省略 :443）
# 生產／Compose：Nginx 對外若為 443，開 https://localhost，API 仍為同 origin 的 /api，通常維持空字串即可
# 本機直連後端（繞過 Nginx）範例：VITE_API_BASE=http://127.0.0.1:8080
# 若 Nginx 改對外埠（非 443），請用完整 origin，例如：VITE_API_BASE=https://localhost:8443
VITE_API_BASE=
